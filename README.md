# oracle-apex-disabled-dates
Oracle APEX solution that dynamically disables fully booked ticket dates in the Date Picker. It marks dates as unavailable, prevents users from selecting them, and automatically reapplies the logic when the calendar is refreshed or re-rendered.
